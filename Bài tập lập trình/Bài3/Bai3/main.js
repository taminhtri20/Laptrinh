let a= Number(prompt("so tien ban tiet kiem"));
let b= Number(prompt("so nam"));
let c= Number(prompt("so lai suat trong mot nam"));
let y=a*c;
var z=b*y;
function M() {
    z+=a
   document.write("tong ca goc va lai:"+z, "VND") 
}